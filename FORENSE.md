# Forense Tools

```bash
# PDF utilities
sudo apt install poppler-utils

# Get PDF Metadata
pdfinfo document.pdf

# EXIF tools
sudo apt install libimage-exiftool-perl

# Get EXIF Metadata
exiftool image.jpg
```